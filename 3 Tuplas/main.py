"""
LOS DATOS DE UNA TUPLA SON INMUTABLES
Las tuplas guardan direferentes tipos de datos. Tambien podemos almacenar otras tuplas.
Las Tuplas se manejan por indices y comienzan por "0", por medio de los indices podemos
consultar los valores almacenados.

La Tupla es más rápido a la hora de dar informacion ya que python almacena un espacio en memoria
solo para esto, las listas no.
"""
#           0      1    2      3      4           5
#  tupla = ('String', 10, 23.3, True, [1, 2, 3], (4, 5, 6))

# print(tupla)
# Con type verificamos que clase es el dato
# print(type(tupla))
